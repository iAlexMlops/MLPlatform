argo-workflows:
  crds:
    install: true
  server:
    ingress:
      enabled: true
      ingressClassName: "nginx"
      hosts:
        - keycloak.alex.localhost
      paths:
        - /
      pathType: Prefix

    authMode: server