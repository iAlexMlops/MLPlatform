apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: argocd-vs
  namespace: argocd
spec:
  hosts:
  - keycloak.alex-localhost.ru
  gateways:
  - istio-system/platform-gateway
  tls:
  - match:
    - port: 443
      sniHosts:
      - keycloak.alex-localhost.ru
    route:
    - destination:
        host: keycloak
        port:
          number: 80
  http:
  - route:
    - destination:
        host: keycloak
        port:
          number: 80