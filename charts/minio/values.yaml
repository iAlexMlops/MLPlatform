minio:
  ingress:
    enabled: false

  auth:
    rootUser: admin
    rootPassword: password

  provisioning:
    enabled: true
    buckets:
    - name: loki
      quota:
        type: set
        size: 3GiB
      tags:
        app: loki