apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: minio-vs
spec:
  hosts:
  - minio.alex.localhost
  gateways:
  - istio-system/platform-gateway
  tls:
  - match:
    - port: 443
      sniHosts:
      - minio.alex.localhost
    route:
    - destination:
        host: minio
        port:
          number: 80
  http:
  - route:
    - destination:
        host: minio
        port:
          number: 80