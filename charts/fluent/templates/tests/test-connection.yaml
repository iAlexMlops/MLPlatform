apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fluent.fullname" . }}-test-connection"
  labels:
    {{- include "fluent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fluent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
