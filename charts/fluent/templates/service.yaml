apiVersion: v1
kind: Service
metadata:
  name: {{ include "fluent.fullname" . }}
  labels:
    {{- include "fluent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fluent.selectorLabels" . | nindent 4 }}
